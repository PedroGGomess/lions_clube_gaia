# ============================================
# REQUIRED FOR VERCEL DEPLOYMENT
# ============================================

# Supabase Configuration (REQUIRED for production/Vercel)
# Get these values from: Supabase Dashboard > Your Project > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin Login Credentials (REQUIRED)
# These credentials are used for /admin/login
# Default credentials for Lions Clube de Gaia
ADMIN_USERNAME=LionsClubeGaia
ADMIN_PASSWORD=Lionsclubegaia@

# ============================================
# LOCAL DEVELOPMENT ONLY
# ============================================

# Database (for local development only - NOT used on Vercel)
# Vercel deployment uses Supabase (PostgreSQL) instead of SQLite
DATABASE_URL="file:./dev.db"

# ============================================
# OPTIONAL
# ============================================

# Rate limiting (requests per minute)
RATE_LIMIT_MAX=10
