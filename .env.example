# Supabase (REQUIRED for production/Vercel deployment)
# For local development, you can use a Supabase project or skip if only testing with Prisma
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Admin credentials (used for admin panel login)
# NOTE: Update these values in your .env file with secure credentials
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-admin-password

# Database (for local development only - NOT used on Vercel)
# Vercel uses Supabase (PostgreSQL) instead of SQLite
DATABASE_URL="file:./dev.db"

# Optional: Rate limiting (requests per minute)
RATE_LIMIT_MAX=10
